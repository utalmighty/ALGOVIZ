
// makes all the element in array int type.
function intparser(arr) {
    for (var a = 0; a < arr.length; a++) {
         arr[a] = parseInt(arr[a]);
    }
    return arr;
}
// BY UTKARSH JAISWAL